# We use -mno-gpopt for two purposes: it allows libraries to be used in
# situations where $gp != our _gp, and it allows them to be built with -G8
# while retaining link compatibility with -G0 and -G4.
CFLAGS_FOR_TARGET += -mno-gpopt
CXXFLAGS_FOR_TARGET += -mno-gpopt
